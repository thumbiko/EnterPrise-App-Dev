Instructions to execute code.

i. You need to have the nodejs installed.
ii. Install the folder and unzip contents.
iii. Open terminal then CD where the code is installed.
iv. Run the command 'node server.js ' to start the server.
v.



Completed parts

2. Part two is completed but however it gives an error when i run the code in the node server
but when i run the code in a live server extension on vs code. The json file contents are displayed.
3.
